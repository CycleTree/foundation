---
interface Props {
  title: string;
  description?: string;
}

const { title, description = "mtdnot's blog" } = Astro.props;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} | mtdnot</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
  </head>
  <body>
    <div class="scanlines"></div>
    <div class="vignette"></div>
    
    <header>
      <nav>
        <a href="/" class="logo">◈ mtdnot</a>
        <div class="nav-links">
          <a href="/about">About</a>
          <a href="/anag">ANAG</a>
          <a href="/refixa">Refixa</a>
          <a href="/posts">Posts</a>
        </div>
      </nav>
    </header>
    
    <main>
      <slot />
    </main>
    
    <footer>
      <p>© 2026 mtdnot / ANAG / Refixa</p>
    </footer>
  </body>
</html>

<style is:global>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  :root {
    --bg: #0a0a0c;
    --bg-panel: #0d0d10;
    --border: #1a1a20;
    --text-dim: #4a4a55;
    --text: #9a9aa5;
    --text-bright: #e0e0e8;
    --accent: #8bc34a;
    --accent-2: #00a5a5;
  }
  
  html {
    font-family: 'Noto Sans JP', sans-serif;
    background: var(--bg);
    color: var(--text);
    line-height: 1.8;
  }
  
  body {
    min-height: 100vh;
    position: relative;
  }
  
  /* CRT Effects */
  .scanlines {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(0, 0, 0, 0.1) 2px,
      rgba(0, 0, 0, 0.1) 4px
    );
    pointer-events: none;
    z-index: 9999;
  }
  
  .vignette {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%);
    pointer-events: none;
    z-index: 9998;
  }
  
  /* Header */
  header {
    border-bottom: 1px solid var(--border);
    padding: 16px 0;
  }
  
  nav {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 24px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .logo {
    font-family: 'JetBrains Mono', monospace;
    font-size: 18px;
    font-weight: 700;
    color: var(--accent);
    text-decoration: none;
    letter-spacing: 1px;
  }
  
  .nav-links {
    display: flex;
    gap: 24px;
  }
  
  .nav-links a {
    color: var(--text-dim);
    text-decoration: none;
    font-size: 14px;
    letter-spacing: 0.5px;
    transition: color 0.2s;
  }
  
  .nav-links a:hover {
    color: var(--text-bright);
  }
  
  /* Main */
  main {
    max-width: 900px;
    margin: 0 auto;
    padding: 48px 24px;
  }
  
  /* Typography */
  h1 {
    font-size: 32px;
    font-weight: 700;
    color: var(--text-bright);
    margin-bottom: 16px;
    letter-spacing: 1px;
  }
  
  h2 {
    font-size: 24px;
    font-weight: 700;
    color: var(--text-bright);
    margin-top: 48px;
    margin-bottom: 16px;
    padding-bottom: 8px;
    border-bottom: 1px solid var(--border);
  }
  
  h3 {
    font-size: 18px;
    font-weight: 700;
    color: var(--accent);
    margin-top: 32px;
    margin-bottom: 12px;
  }
  
  p {
    margin-bottom: 16px;
    color: var(--text);
  }
  
  a {
    color: var(--accent);
    text-decoration: none;
  }
  
  a:hover {
    text-decoration: underline;
  }
  
  ul, ol {
    margin-bottom: 16px;
    padding-left: 24px;
  }
  
  li {
    margin-bottom: 8px;
  }
  
  code {
    font-family: 'JetBrains Mono', monospace;
    background: var(--bg-panel);
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 14px;
  }
  
  pre {
    background: var(--bg-panel);
    padding: 16px;
    border-radius: 8px;
    overflow-x: auto;
    margin-bottom: 16px;
    border: 1px solid var(--border);
  }
  
  pre code {
    background: none;
    padding: 0;
  }
  
  blockquote {
    border-left: 3px solid var(--accent);
    padding-left: 16px;
    margin: 24px 0;
    color: var(--text-dim);
    font-style: italic;
  }
  
  /* Footer */
  footer {
    border-top: 1px solid var(--border);
    padding: 24px;
    text-align: center;
    color: var(--text-dim);
    font-size: 12px;
  }
  
  /* Article specific */
  .article-header {
    margin-bottom: 48px;
  }
  
  .article-meta {
    color: var(--text-dim);
    font-size: 14px;
    margin-bottom: 8px;
  }
  
  .toc {
    background: var(--bg-panel);
    border: 1px solid var(--border);
    padding: 16px 24px;
    margin-bottom: 32px;
    border-radius: 8px;
  }
  
  .toc h4 {
    font-size: 12px;
    color: var(--text-dim);
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 12px;
  }
  
  .toc ul {
    list-style: none;
    padding-left: 0;
    margin: 0;
  }
  
  .toc li {
    margin-bottom: 6px;
  }
  
  .toc a {
    color: var(--text);
    font-size: 14px;
  }
  
  .toc a:hover {
    color: var(--accent);
  }
</style>
